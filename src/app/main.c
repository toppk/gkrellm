
#include "gui.h"

/* 
   -drop shadows for windows when decorations are off
   * load theme
   * display theme
   * display monitors
   * load monitors
   * input / hotkeys / move window / activate krells?
   * configuration
   * plugins
   * command line
   * client

*/

int main(int argc,
		 char **argv)
{
	return gk_start_ui();
}
